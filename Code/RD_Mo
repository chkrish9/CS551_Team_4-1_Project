const mongoose = require('mongoose');
const Schema = mongoose.Schema;

//Area schema
const AreaModel = mongoose.Schema({
    name: {
        type: String,
        required: true
    },
    description: {
        type: String,
        required: true
    },